# MOD-A　對話／劇情系統

- Task 版本: v1.2
- 狀態: 實作中（程式碼與資料已完成；2026-07-18 追加對話資料 .tres 化，見下方更新段；實機 smoke test
  依 John 慣例等指示才驗）
- 對應 GDevelop 系統: DEV_開發指南.md L48「對話/劇情佇列」
- 規格來源: `specs/DIALOGUE_SPEC.md` D-1、D-2、D-3（本次實作時發現 D-3/D-8 有缺漏並回填，版本升到
  v1.1，見該檔案版本記錄）

## 2026-07-18 更新：對話資料 .tres 化（v1.2）

John 拍板把對話資料從 run-time parse `dialogue.json` 改為原生 .tres（真相源＝
`resources/content/dialogue/**/*.tres`，比照 content_db.tres）。權威規格見 `specs/DIALOGUE_SPEC.md` v3.0。

- 新增類別（`scripts/dialogue/`）：`CutsceneLine`（過場一句台詞 speaker/text）、`NpcDialogue`
  （單一 NPC 的有序 entries）、`DialogueDatabase`（聚合入口）。
- 改：`CutsceneEntry.lines` → `Array[CutsceneLine]`＋新增 `id`（**破壞性**，spec 升 v3.0）；
  `DialogueSystem._load()` 改讀 `dialogue_db.tres`（不再 `JSON.parse`），內部 `_dlg`/`_cuts` 結構與下游
  API 全部不變。
- 新增轉檔腳本 `scripts/dialogue/build_dialogue_tres.gd`（dialogue.json → 個別 .tres ＋聚合，聚合檔用
  `take_over_path` 產出 ExtResource 引用，故編個別 .tres 即生效）。
- 產出：`resources/content/dialogue/npc/<id>.tres`×10、`cuts/<id>.tres`×8、`dialogue_db.tres`×1
  （headless 產出 EXIT 0，Godot 能 parse 全部新 class 並實例化）。
- dialogue.json 降級為種子（同 content.json 地位）。設計員之後直接在 Inspector 編個別 .tres。
- **尚未做**：實機 smoke test 驗 autoload 啟動時 `_load()` 正確載入聚合檔（依 John 慣例等指示才驗）。

---

## 驗收現況（2026-07-13，MOD-A worktree）

- **產出檔案**（皆已寫入 `godot-crystal-tales-mod-a` worktree、`mod-a` 分支）：
  - `godot-project/scripts/dialogue/extract_dialogue.py`：對 `build_cq2.py` 做 AST 解析
    （`ast.literal_eval`）抽取 DLG/CUTS，不是手動抄寫；可重跑。
  - `godot-project/resources/content/dialogue.json`：抽取結果，`dlg`（10 個 npc_id、共 38 條對話條目）
    ＋ `cuts`（8 筆過場）。
  - `godot-project/scripts/dialogue/dialogue_entry.gd`／`cutscene_entry.gd`：對應 Resource 類別。
  - `godot-project/scripts/dialogue/dialogue_system.gd`：`open_npc_dialogue()`／`play_cutscene()`／
    `advance()`／`show_message()`／`play_defeat_narration()`，14 個 action id 的完整副作用（逐條核對
    `build_cq2.py` L1758-1779，非憑對話文字猜測）。註冊為新 autoload `DialogueSystem`（見
    `project.godot` 變更說明，理由同 CORE-2 先例）。
  - `godot-project/scenes/ui/dialogue_box.tscn` + `dialogue_box.gd`：最簡陋堪用版對話框（純文字，
    Panel + 兩個 Label），訂閱 `DialogueSystem` 的 signal 更新畫面，等 MOD-D 換裝。
  - `godot-project/scripts/dialogue/verify_dialogue.py`：獨立驗證腳本（見下方）。
- **驗證方式與結果**（誠實記錄：**沒有實機驗證**，原因見 `TASKS/00_核心任務.md` CORE-1 段落——這次
  worktree 環境同樣拿不到 Godot 4.3+ 執行檔）：
  1. `python3 scripts/dialogue/verify_dialogue.py`：**全部通過**。內容包含 (a) 對 `build_cq2.py` 做
     第二次獨立 AST 抽取（不共用 `extract_dialogue.py` 的程式碼路徑）逐筆比對 `dialogue.json`，DLG
     38 條 / CUTS 8 筆完全一致（含這次新發現的 `battle`/`transfer`/`setstep`/`party` 欄位）；(b) 14 個
     `dialogue.json` 用到的 action id 全部在 `dialogue_system.gd` 有對應分支（`give_earring` 有實作
     但目前資料沒用到，非缺漏，是原始碼本來就有的死碼，保留供對稱性）；(c) 抽樣緹娜/漢克/老葛雷在多種
     旗標組合下選中的對話分支跟原始碼陣列順序一致（含「未定義旗標視為 0」的邊界情況）。
  2. `python3 /tmp/.../check_gd_syntax.py`（輕量括號配對＋縮排一致性檢查，非真正 GDScript parser）：
     `dialogue_entry.gd`/`cutscene_entry.gd`/`dialogue_system.gd`/`dialogue_box.gd` 全部 `[OK]`。
  3. `dialogue_box.tscn`：手動核對 `[gd_scene]`/`[ext_resource]`/`[node]` 區塊格式與 `title.tscn`
     一致，`ext_resource` 引用的 `dialogue_box.gd` 路徑存在。
  4. **未做到、且不能假裝做到的**：`godot --headless --check-only` 沒有執行過（沒有 Godot 執行檔）；
     `DialogueSystem` 這個新 autoload 有沒有在 Godot 4.3 引擎裡正確初始化、`class_name` 全域解析、
     signal 訂閱時機是否正確，都還沒有實機驗證。
- **對 MOD-B `flag_matcher.gd` 的依賴**：`dialogue_system.gd` 直接呼叫
  `FlagMatcher.matches(flags, when)`（`res://scripts/dialogue/flag_matcher.gd`，D-1 簽名），**這個檔案
  在本 worktree 執行當下不存在**（依協調者指派，固定由 MOD-B 建立）。這是刻意的、預期中的未解析依賴，
  合併 `mod-a`／`mod-b` 兩個分支後才會補上，不代表 `dialogue_system.gd` 有 bug。
- **對 CORE-3 `SaveManager` 的處理**：CORE-3 目前連暫時性 stub 都沒有（不像 GameState/SceneRouter）。
  為了不讓 `dialogue_system.gd` 的可解析性綁在一個不存在的 autoload 上，存檔呼叫改用
  `get_node_or_null("/root/SaveManager")` 動態查找＋`has_method()` 檢查，找不到就靜默跳過。CORE-3
  正式把 `SaveManager` 註冊進 `[autoload]` 後不需要改本檔案任何一行就會自動生效。
- **對 MOD-F `derive.gd`（衍生屬性）的已知限制**：`heal` action 與過場 `party` 欄位（新成員加入隊伍）
  刻意不重算 `maxhp`/`maxmp`/`patk`/... 等衍生屬性（那是 MOD-F 的職責，見
  `TASKS/11_並行協作規則.md` 衝突矩陣「不允許任何模組自己重算衍生屬性」）。`heal` 只信任既有 Dictionary
  裡已經有的 `maxhp`/`maxmp` 欄位，缺的話就維持原值不變，不會報錯但也不會真的回滿；新加入的隊伍成員
  Dictionary 沒有這些欄位。MOD-F 任務完成、有能呼叫的 derive 等價物之後，這裡應該回頭補上正確呼叫（目前
  故意不用「自己抄一份公式」的方式繞過，避免跟 MOD-F 各寫一份）。
- **待人工決定的事項**：無阻塞性的，但列出兩點供 John／協調者參考：
  1. 是否要在 CORE-3/MOD-F 完成後，回來把 `dialogue_system.gd` 的 `_try_save()`／`_heal_all()` 的
     「動態查找＋已知限制」寫法，換成硬相依（跟 `FlagMatcher` 一樣直接假設存在）？目前選擇動態查找是因為
     SaveManager 連 stub 都沒有，跟 FlagMatcher（已明確指派＋預期會存在）情況不同；等 CORE-3 真的完成後，
     這個防禦寫法可以留著（無害）也可以拿掉，非急迫。
  2. `dialogue_box.tscn` 目前是純文字+單色 Panel，沒有背景圖／立繪（`DlgArt`/`DlgFace` 對應的美術資產
     不在本次範圍），MOD-D 換裝時可以直接砍掉重做，不需要相容這版的節點結構。

## 目標

移植 NPC 對話（`DLG`）與過場劇情（`CUTS`）系統：旗標比對選台詞、對話框逐句播放、`action` 副作用執行。

## 產出

- `godot-project/scripts/dialogue/dialogue_system.gd`：`open_npc_dialogue(npc_id)`、`play_cutscene(cut_id)`、
  逐句推進（對應 `st.dlgIdx`/`st.cutIdx`）。
- `godot-project/scripts/dialogue/flag_matcher.gd`：`specs/DIALOGUE_SPEC.md` D-1 的純函式實作（MOD-B 共用，
  誰先開工誰先建立，另一方直接引用，不要各寫一份）。
- `godot-project/resources/content/dialogue.json`（或 `.tres`）：從 `build_cq2.py` L931-990+ 抄出的 DLG/CUTS
  資料，附抽取腳本或手動轉寫紀錄。
- 每個 `action` id 的副作用實作（`register`/`ch1_take`/`ch1_reward`/`ch2_take`/`ch2_report`/`heal`/
  `give_ring`/`shop_hank_gift`/`shop_gid_gift`/`mira_start`/`mira_reward`/`relic_turnin`/`shop_hank`/
  `shop_gid` — 完整清單以實際回讀原始碼為準，不要只憑本檔案列的清單，可能有遺漏）。
- 對話框 UI 場景 `godot-project/scenes/ui/dialogue_box.tscn`（畫面呈現部分若 MOD-D 已建立通用文字框元件，
  這裡直接複用，不要重複做一個)。

## 前置依賴

CORE-1、CORE-2、CORE-4（讀寫 `GameState.flags`/`GameState.itemInv`/`GameState.gold`）、CORE-5（部分 action
會觸發存檔或轉場，如 `register` 之後可能有 UI 提示但不轉場；純粹保險起見依賴 CORE-5 API 存在）。

## 驗收標準

- 抽出的 `dialogue.json` 資料筆數與 `build_cq2.py` 現有 `DLG`/`CUTS` 條目數一致（人工核對一次）。
- 每個 NPC 在不同旗標狀態下對話內容與 GDevelop 版一致（抽樣至少涵蓋緹娜/漢克/老葛雷三位，因為他們的分支
  最多）。
- 每個 `action` 的旗標/道具/金幣異動跟原始碼一致。

## 擁有檔案

`godot-project/scripts/dialogue/**`、`godot-project/resources/content/dialogue.json`、
`godot-project/resources/content/dialogue/**`（v1.2 .tres 化後的真相源）。
`flag_matcher.gd` 若 MOD-B 先建立則由 MOD-B 擁有，MOD-A 只能呼叫不能改（見 `11_並行協作規則.md`）。

## 已知風險

- DLG/CUTS 目前混在 3487 行的 `build_cq2.py` 裡，抽取時容易漏抄或抄錯行，建議寫一支一次性 Python 腳本
  直接 parse `build_cq2.py` 的 `DLG =`/`CUTS =` dict literal 並輸出 JSON，而不是手動複製貼上（人工抄容易
  出錯，且 GDevelop 端之後若還有台詞異動，重跑腳本就能重新抽取）。
