# MOD-F　衍生屬性／戰鬥公式引擎

- Task 版本: v1.1
- 狀態: 實作中
- 對應 GDevelop 系統: WORLD_JS 與 BATTLE_JS 各一份的 `derive()`（DEV_開發指南.md L63：「改公式要同步兩處」
  ——這是本模組刻意要修掉的技術債）
- 規格來源: `specs/BATTLE_FORMULAS.md` F-1、F-2（全文）

## 驗收現況（2026-07-14，MOD-F worktree）

- **產出檔案**（皆已寫入 `godot-crystal-tales-mod-f` worktree、`mod-f` 分支）：
  - `godot-project/scripts/content/derive.gd`：`Derive.derive(member: Dictionary) -> Dictionary`，完整
    實作 F-1 全部欄位（maxhp/maxmp/patk/matk/pdef/mdef/dodgeV/critV/spd，含 `_eq_stat()` 裝備加成、
    `m.eq` 未定義時套用 `startEq` 模板、hp/mp clamp、`sk` 技能表初始化、`spts` 初值）。合併 GDevelop
    版 WORLD_JS（build_cq2.py L1326-1345）與 BATTLE_JS（L2641-2661）兩份重複實作為唯一一份，並依
    F-1 註記統一採用 **critV 取一位小數**（WORLD 版寫法）——這是本次遷移唯一一處相對 GDevelop 現況
    的刻意行為修正，修正點在檔案內以醒目註解標註（`_eq_stat` 旁與 critV 那行）。
  - `godot-project/scripts/content/exp_need.gd`：`ExpNeed.exp_need(lv: int) -> int`，對應 F-2
    （L1325/L2662，兩處逐字相同，無行為差異，純粹收斂成一份）。
  - `godot-project/scripts/content/test_derive.py`：純 Python 單元測試（見下方「驗證方式與結果」）。
  - **不修改** `autoload/game_state.gd`／`scripts/dialogue/dialogue_system.gd`（CORE-4／MOD-A 擁有），
    確認過 `dialogue_system.gd` `_make_party_member()` 產生的 Dictionary 欄位形狀
    （`id/name/cls/mainAttr/sprite/guest/lv/exp/pts/spts/attrs/eq`）跟 `derive()` 預期的輸入形狀相容，
    整合呼叫留給後續任務。
- **驗證方式與結果**（誠實記錄：**沒有實機驗證**，原因同 CORE-1／MOD-A——這個 worktree 環境同樣拿不到
  Godot 4.3+ 執行檔）：
  1. `python3 godot-project/scripts/content/test_derive.py`：**全部通過（FAIL: 0）**。方法是把
     `derive.gd`/`exp_need.gd` 逐行翻譯成 Python（`py_derive()`/`py_exp_need()`），對照凍結的 fixture
     ——fixture 來源是**直接執行**從 `build_cq2.py` 逐字抄出的 WORLD 版／BATTLE 版 `derive()`/
     `expNeed()` 字面 JS 原始碼（Node.js v22），而不是憑印象手算期望值。
  2. 測試涵蓋 **3 名不同 mainAttr 的隊伍成員**：`ludo`（str，真實角色，無裝備/滿裝備+Lv5 升級點各一）、
     `marin`（agi，真實角色，`m.eq` 未定義觸發 `startEq` 模板分支）、`aaron`（str，真實 guest 角色，
     `startLevel=20` 高等級邊界）。**CONTENT.json 目前沒有 int 主屬性角色**（截至本次實作只有
     ludo/marin/aaron 三名成員，mainAttr 分別是 str/agi/str），這是資料現況、不是本任務遺漏——為了讓
     `attrs[mainAttr]` 的通用索引邏輯與 matk/mdef 的 int 分支有測試覆蓋，額外加了一筆明確標記為合成的
     `synthetic_mage`（int，`id` 不存在於 CONTENT.json，裝備/技能表仍查真實資料），測試檔案開頭
     `_SYNTHETIC_NOTE` 有記錄這個資料缺口。另外涵蓋技能解鎖等級邊界（`whirl` unlockLv=5，Lv4/Lv5 各一）、
     hp/mp clamp（舊存檔數值超過上限、hp 完全未定義兩種情境）、`sk` 已存在但為空 dict `{}` 時不可重新
     計算（JS `if(!m.sk)` 的 truthy 語意，空物件在 JS 是 truthy，逐字複製這個細節）。
  3. **critV 刻意修正點的正面驗證**：測試案例中挑出 3 筆（`marin_default_eq`／`aaron_guest_lv20`／
     `ludo_skill_boundary_lv4`）WORLD 版跟 BATTLE 版算出來的 critV 因浮點誤差確實不同（例如
     `marin_default_eq`：WORLD=10.9、BATTLE=10.85），斷言 `derive.gd` 的結果對齊 WORLD、不等於
     BATTLE，證明修正真的生效，不是巧合對上舊行為。
  4. `test_derive.py --regen`：額外現場用 Node.js 重跑同一段 build_cq2.py 字面原始碼，核對凍結 fixture
     沒有因為 `build_cq2.py` 之後被改動而過期——這台機器上有 Node.js v22（`/opt/node22/bin/node`），
     `--regen` 模式全部通過。
  5. 手動檢查 `derive.gd`/`exp_need.gd` 括號/中括號/大括號配對平衡、縮排一律用 tab（跟其他 `.gd` 檔案
     風格一致）。**未做到**：`godot --headless --check-only` 沒有執行過（沒有 Godot 執行檔），
     `class_name Derive`/`ExpNeed` 全域解析、`ContentDB`/`DerivedParams`/`EquipmentDef`/`SkillDef`/
     `PartyMemberDef` 型別標註是否跟引擎實際行為一致，都還沒有實機驗證。
- **對 ContentDB 的依賴**：`derive.gd`/`exp_need.gd` 直接呼叫 `ContentDB.get_derived()`/
  `get_party_member()`/`get_equipment()`/`get_all_skills()`（CORE-2 已提供的公開介面），未新增/修改
  `content_db.gd` 或任何 `*_def.gd`。
- **待人工確認**（見下方「已知風險」）：critV 取一位小數的修正屬於純技術債清理，理論上不影響玩家可見
  數值範圍（原本兩版本來就該一致，只是沒人發現漂移），但這是本次遷移唯一一處刻意行為偏離，`MIGRATION_
  OVERVIEW.md` 進度表打勾前建議 John 過目一次確認沒問題（本任務不代為修改 `MIGRATION_OVERVIEW.md`）。

## 目標

**唯一一份**衍生屬性計算（取代 GDevelop 版重複維護的兩份），供 MOD-C（世界場景 HUD 顯示屬性）與 MOD-E
（戰鬥數值）共用。

## 產出

- `godot-project/scripts/content/derive.gd`：純函式 `derive(member: PartyMemberData) -> PartyMemberData`，
  完整實作 `specs/BATTLE_FORMULAS.md` F-1（maxhp/maxmp/patk/matk/pdef/mdef/dodgeV/critV/spd 全部欄位，
  含裝備加成 `eqStat`、`critV` 取一位小數的刻意統一寫法）。
- `godot-project/scripts/content/exp_need.gd`：F-2 公式。
- 單元測試：至少覆蓋「無裝備」「滿裝備」「等級邊界」三種情境，數值跟 GDevelop 版現有角色實測比對（在
  GDevelop 版用 debug hook `window.__W`/`__B` 讀出實際數值當作測試 fixture，寫進測試檔案）。

## 前置依賴

CORE-2（需要 `ContentDB` 提供 `CONTENT.derived` 係數表與裝備資料）。

## 驗收標準

- 抽樣至少 3 名隊伍成員（不同 `mainAttr`/職業）在多個等級點的衍生數值，跟 GDevelop 版 debug hook 讀出的
  實測值一致（誤差為 0，因為是確定性公式，不接受「差不多」）。
- MOD-E、MOD-C 兩邊都改成呼叫這一份，不允許任何模組自己重算衍生屬性。

## 擁有檔案

`godot-project/scripts/content/derive.gd`、`exp_need.gd`。

## 已知風險

- 這是**唯一一個相對於 GDevelop 現況做刻意行為修正**（合併兩份 `derive()`、統一 `critV` 取一位小數）的
  模組，實作前務必跟 John 確認這個修正沒問題（理論上是純技術債清理、不影響玩家可見數值，但穩妥起見在
  `MIGRATION_OVERVIEW.md` 進度表勾選前先過一次目視確認）。
