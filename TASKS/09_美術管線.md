# MOD-I　美術／音效匯入管線

- Task 版本: v1.1
- 狀態: 實作中（2026-07-14 資產複製＋雜湊驗證完成；import 行為因環境無 Godot 執行檔未實機驗證，見下方「實作現況」）
- 對應 GDevelop 系統: `art_v2.py`/`art_v3_lpc.py`/`art_v6_chars.py`/`art_v8_foes.py`/`art_v9_creatures.py`/
  `art_v10_npcwalk.py`/`art_v12_furniture.py`/`art_v13_title.py`/`art_v14_forest.py`（全部是 PIL 圖像合成，
  跟輸出引擎無關，見 `MIGRATION_OVERVIEW.md`「可高度複用」表）

## 目標

把現有美術資產（593 PNG + 17 音效）複製進 Godot 專案並設定正確的 import 設定（像素風格：Filter=Nearest，
不產生 mipmap），美術生成腳本的**合成邏輯保留**，只需要調整輸出路徑，不需要重寫圖像處理演算法。

## 產出

- `godot-project/assets/` 下與 `../gd-crystal-tales/projects/crystal-quest/assets/` 結構一致的複製（
  `char/`/`battle/`/`props/`/`ui/`/`sfx/`/`bgm/`/`map/`），可用同步腳本或一次性複製，複製方式待 CORE-1
  定案（是否用 git submodule/symlink 或直接複製，記錄決策於本檔案）。
- `.import` 設定確認：所有像素美術的 `Filter` 關閉（對應 GDevelop 的 `scaleMode: nearest`），Godot 專案
  設定裡的預設 texture import preset 先調好，避免每張圖手動設定。
- 音效 `mp3`/`wav` 直接複製，Godot 原生支援兩種格式，不需轉檔。
- 室內大圖立繪系統（`intc_<key>` 手繪背景 + `IntArt` 立繪前景，DEV 指南 L88）：Godot 端用
  `TextureRect`/`Sprite2D` 疊圖還原，兩種模式（`mode:"menu"` 立繪選單 / 預設 walk 可走動）都要支援。

## 前置依賴

CORE-1；跟大部分模組平行無依賴衝突（純資產複製與 import 設定），但 MOD-D（室內立繪選單模式的 UI 呈現）
會消費本模組產出。

## 驗收標準

- 抽樣比對複製後的圖檔與原始檔案雜湊一致（不是重新繪製，是原樣複製）。
- 像素圖在 Godot 編輯器內預覽無模糊（Filter 設定正確）。
- `CREDITS_素材授權.md` 的授權義務隨資產複製一併帶到 Godot 專案（複製一份到 `godot-crystal-tales/`，
  之後兩邊素材若各自演進要分別維護，見 `CLAUDE.md` 版本規則）。

## 擁有檔案

`godot-project/assets/**`、Godot 專案的 import 預設設定。

## 實作現況（2026-07-14，MOD-I，branch `mod-i`）

### 已完成

- **複製方式決策**（本檔案「產出」第一點要求記錄）：採**直接複製進 repo**（非 submodule/symlink），
  與 `CLAUDE.md` 目錄地圖規劃一致；約 72MB 二進位資產直接 git 管理，此專案規模合理。
- 複製 `../gd-crystal-tales/projects/crystal-quest/assets/` → `godot-project/assets/`，
  目錄結構原樣（`map/ char/ battle/ props/ ui/ sfx/ bgm/`），共 **612 檔／72MB**：
  map 2、char 280、battle 156（含 `src_foes/` 子目錄與 `hero_dims.json`/`manifest.json` 兩個
  中繼資料 JSON，一併原樣複製）、props 90、ui 67、sfx 12、bgm 5。
- `map/*.tmj`（6 個 Tiled 地圖檔）**依規劃不複製**——Godot 版由 MOD-H 直接生成 `.tscn`；
  `map/atlas.png`、`map/atlas_forest.png` 圖磚集有複製（MOD-H 引用 `res://assets/map/atlas.png`）。
- 檔名掃描：**無空格檔名**（GDevelop 歷史坑已清，無異常）。
- 驗證：**全量（612/612）md5 雜湊與來源逐檔比對一致**（不只抽樣），檔案清單 diff 為空。
- `CREDITS_素材授權.md` 複製到 repo 根目錄，檔頭加註「Godot 遷移副本、兩邊各自維護」說明。
- `godot-project/.gitignore` 確認：`.godot/`（含 import 快取）已排除；`*.import` sidecar 檔
  不受 `.import/`（目錄規則）影響、會進版控（Godot 4 慣例，正確）。
- `art_v*.py` 生成腳本依「已知風險」決議**不搬入** Godot 專案；wolf 佔位圖**原樣複製未重繪**。

### import 設定處理（重要：部分未經編輯器驗證）

- 像素 texture 的 Filter=Nearest 由 `project.godot` 全域預設
  `rendering/textures/canvas_textures/default_texture_filter=0` 承擔（CORE-1 已設）；個別
  `*.import` 檔由 Godot 編輯器首次掃描時自動產生，本環境無 Godot 執行檔，**不預寫**幾百個 texture
  import 檔。**首次在有 Godot 4.3 編輯器的環境開啟專案時，會自動產生所有 import 檔，屆時要抽樣確認
  像素圖預覽無模糊（Filter 繼承專案預設 Nearest）**。
- 例外：`assets/bgm/*.mp3.import` 共 5 檔為 MOD-I **手寫**（BGM 需循環，預設 loop=false 不符需求），
  格式依 Godot 4.3 `ResourceImporterMP3` 參數（loop/loop_offset/bpm/beat_count/bar_beats），刻意
  省略 uid 與 [remap] path 讓編輯器首次匯入時自行補齊。**未經編輯器驗證**——首次開啟專案後需確認
  loop=true 有保留、BGM 實際循環；若編輯器拒讀該格式，刪掉這 5 檔讓它重生、再於匯入面板手動勾 loop 即可。
- sfx 的 wav/mp3 用預設匯入參數（不循環），不需手寫。

### 未完成（留給後續）

- 「產出」第 4 點的室內大圖立繪**場景組裝**（TextureRect/Sprite2D 疊圖、menu/walk 兩模式）不在本次
  範圍——本次僅原樣複製 `int_*`/`intc_*`/`portrait_*` 圖檔，場景還原由 MOD-D/MOD-H 或後續任務處理。
- 編輯器實機驗證（Filter 無模糊、bgm loop）→ 驗收標準第 2 點未閉環，故狀態停在「實作中」而非「已驗收」。
- 來源 CREDITS 內部有一處待釐清（複製時未代改）：檔案開頭「角色」段仍標註 wolf 為熊 recolor 佔位，
  但「敵人戰鬥圖」段又寫野狼已改用 LPC Wolf Animation（取代佔位）——以 GDevelop 端為權威，待該端
  文件釐清後再同步措辭；不影響資產本體（原樣複製、雜湊一致）。

## 已知風險

- `wolf`（野狼）目前是熊剪影去飽和的佔位圖（`CREDITS_素材授權.md` 已標註待補正式素材），遷移時**原樣複製
  佔位圖**，不要在遷移過程中「順便」重繪，正式素材替換是獨立的美術任務，不要跟引擎遷移的驗收標準混在一起。
- 美術生成腳本（`art_v*.py`）本身**不建議在本次任務裡搬進 Godot 專案**：GDevelop 版仍在持續開發（目前到
  第二章），美術生成腳本應該留在 `gd-crystal-tales` 繼續產圖，Godot 專案只消費產出的 PNG，避免兩邊各自
  維護一份生成邏輯導致分岔。若未來要讓 Godot 版獨立產生新美術，屬於遷移完成後的後續任務，不在本次範圍。
