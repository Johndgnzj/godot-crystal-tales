# 第一章重構施工清單（六小節）

- Task 版本: v1.1
- 對應 GDevelop 原始碼快照: N/A（Godot 端內容重構，不對應 GDevelop 邏輯）
- 狀態: Phase 0 定案、Phase 1 完成；**Phase 2 遭遇骨架完成**（2026-07-19，CH1-04/05 已驗收＋CH1-06 部分；boss 與閘門旗標遷移依相依關係歸 Phase 3）；Phase 3 劇情待逐節審
- 內容來源: [docs/story/第一章劇本草稿.md](../docs/story/第一章劇本草稿.md)（六小節台詞）＋ [故事大綱.md](../docs/story/故事大綱.md)（藍圖）＋ [角色設定.md](../docs/story/角色設定.md)
- 稽核依據: 2026-07-19 完備性稽核（怪/道具/角色/對話/地圖/任務 三方盤點）

> **一句話**：怪、角色、地圖美術、大部分道具都齊了；缺的是「把零件接成一條能從頭打到尾的第一章」。
> 最大的坑：**現行主線走 painted 地圖，但所有戰鬥/boss 還留在主線進不去的舊 tilemap 版**。

## 依賴總覽

```
Phase 0 決策（D1 flag藍圖 / D2 守衛 / D3 東森控制器）  ← 先拍板，擋後面
      │
Phase 1 素材（可並行，無依賴）── items 新建 / 裝備正名 / 清死碼
      │
Phase 2 主線骨架能打（依 D1,D3）── painted 掛遭遇+boss+閘門
      │                                   ▶ 里程碑 M-A：主線可從頭打到尾
Phase 3 劇情血肉（依 D1,D2,Phase1）── 六小節過場重寫 + necro 接線
      │                                   ▶ 里程碑 M-B：六小節劇情完整
Phase 4 整合收尾 ── 串測 / 同步 docs / 封存舊 tile
```

**建議執行策略**：先 Phase 0→1→2 做出「能打的主線」（M-A，可先驗收手感），再 Phase 3 補劇情血肉。不要一次全開。

---

## Phase 0 — 定案（擋路決策，須先拍板）

這三項擋住後面大半工項，先決定。每項附小杰建議。

### CH1-D1　第一章 flag / step 藍圖（線性化）
- **問題**：現行用 `step`(序章) + `ch1`(哥布林) + `ch2`(礦山) 三段旗標，對應舊三段式結構。六小節要重編成單一第一章的線性推進。
- **建議**：用單一線性 `ch1_step`（0→13 對應六小節內各節點），保留 `reg`(登錄)、支線旗標(`herb`/`relic`)。舊 `step/ch1/ch2` 全數退場，過場/閘門/遭遇觸發統一改讀 `ch1_step`。
- **✅ 定案（2026-07-19）**：採線性化 `ch1_step`（0→13）。**完整序列見附錄 A**——這是 Phase 2/3 的接線藍圖。
- **產出**：一張「節點→旗標值→觸發什麼(過場/遭遇/閘門)」對照表，寫進本檔附錄，作為 Phase 2/3 的接線依據。
- **影響**：`autoload/game_state.gd`（flag 介面不變，值語意改）、所有 `dialogue/**/*.tres` 的 `when`、場景 `ExitZone.min_step`。

### CH1-D2　礦山守衛角色（小節5）
- **問題**：小節5「想喝酒的礦山守衛」沒有對應 NPC；原 `guard` 已併入羅素隊長，但羅素是南方大道守備隊長（性格正直、地點不符）。
- **建議**：**新增一個輕量礦山守衛 NPC**（暫定 id `warden`），只需一個 `dialogue/npc/warden.tres`（要酒→喝了放行）。成本低，避免扭曲羅素人設。
- **✅ 定案（2026-07-19）**：新增 `warden` 礦山守衛，立繪暫用通用兵。
- **影響**：新增 `dialogue/npc/warden.tres` + 掛進 `dialogue_db.tres`；立繪可暫用通用兵。

### CH1-D3　東之森 ef_* 控制器路線
- **問題**：北礦 `nm_*` 用 `world_scene.gd`（gameplay 掛鉤齊全，補 export 即可）；東森 `ef_*` 用 `painted_screen.gd`（原型控制器，**完全沒有遭遇/NPC/寶箱/過場掛鉤**）。
- **建議**：**把 `ef_*` 遷移到 `world_scene.gd`**（跟 nm_* 統一），一勞永逸，之後所有東森 gameplay 直接受益；不要維護兩套控制器。一次性成本中等。
- **✅ 定案（2026-07-19）**：`ef_*` 統一遷移到 `world_scene.gd`，`painted_screen.gd` 待廢（收尾階段移除）。
- **影響**：9 張 `ef_*.tscn` 的根節點腳本換綁 + 補 export 欄位；`painted_screen.gd` 之後可廢。

---

## Phase 1 — 素材補齊（可並行，無依賴，工作量小）

### CH1-01　新建 items：烈火蜂蜜、蜂蜜酒　✅ 完成（2026-07-19，headless 驗證載入 PASS）
- 做什麼：`items/` 新增 `fire_honey`（烈火蜂蜜，key/素材）與 `honey_mead`（蜂蜜酒，key），掛進 `content_db.tres`（內嵌 SubResource，非引用個別檔！）。
- 檔案：`resources/content/items/*.tres` + `content_db.tres`
- 驗收：`ContentDB.get_item("fire_honey")` / `get_item("honey_mead")` 非 null（headless 抽查）。

### CH1-02　新建 3 件劇情獨有裝備（漢克的舊劍 / 瑪莎的護身戒 / 冒險者靴）　✅ 完成（2026-07-19，headless 驗證 PASS；數值為暫代衍生維度，待 TASKS/14 屬性任務後換正式 str/agi/luck）
- **做法修正（2026-07-19 查證）**：原訂「正名 `iron_sword`/`vital_ring`/`swift_boots`」不可行——這三件是**通用裝備**（唐恩/吉德商店販售，且 `iron_sword`/`swift_boots` 是亞倫起始裝），正名會讓商店賣劇情道具、亞倫裝備變劇情名。改為**新建 3 件獨有裝備**，通用三件維持不動。
- 做什麼：新建 `hank_blade`(漢克的舊劍/weapon/str)、`martha_ring`(瑪莎的護身戒/acc)、`adventurer_boots`(冒險者靴/boots)；數值參考同期通用裝備、略帶特色，rarity uncommon。
- 檔案：`equipment/*.tres` + `content_db.tres`（內嵌 SubResource + 陣列註冊）。
- 對話指向（Phase 3 連動）：漢克 `give_sword`→`hank_blade`、瑪莎 `give_ring`→`martha_ring`、緹娜登錄→`adventurer_boots`（取得時機從「打完洞熊」移到小節4 登錄）。
- 驗收：headless 抽查三件 `get_equipment` 非 null；商店/亞倫裝備不受影響。

### CH1-03　清 shop_hank 死碼　✅ 完成（2026-07-19，smoke PASS）
- 做什麼：移除漢克的 `shop_hank` 交易項（藍圖鐵鋪是唐恩，漢克不開店）；`dialogue_system.gd` 無此分支、`shops/` 無 hank 資源，屬殘留。
- 檔案：`dialogue/npc/hank.tres`、必要時 `shop.gd`/`dialogue_system.gd` 註解。
- 驗收：漢克對話只剩「贈舊劍＋閒聊」，無 trade 項；無 push_warning。

---

## Phase 2 — 主線骨架能打（依 D1、D3；工作量中）

目標：讓 painted 主線**有敵人、有 boss、能推進**。此階段過場可暫用舊素材/占位，重點是「打得到」。

### CH1-04　北礦 nm_* 掛遭遇 + boss　◑ 遭遇完成（boss 歸 Phase 3）
- 做什麼：`nm_a~nm_f` 補 `world_scene.gd` 的 `enc_group`（依區域深度配 `mine`/`tutorial`）；`nm_f`(boss房) 放 `BossMark` 接 `necro`（小節6 boss）。
- 依賴：D1（flag 決定哪段開遭遇）。
- 驗收：nm_* 走動觸發遭遇；nm_f 進 boss 房打得到死靈術士。
- **✅ 遭遇完成（2026-07-19）**：6 張 nm 皆設 `enc_group="mine"`（John 定：全段用 mine，非 mine/tutorial 分段）＋填滿 `enc_rows`（40×40 全「1」）。**enc_rows 設計決策**：走行區鋪「1」；被碰撞擋的格子玩家腳點永遠站不上（碰撞先擋、`_is_enc_at` 查玩家腳點格）→「全 1」與「只鋪走行區」runtime 等價，且免去解 nm(32px)/ef(38px) 兩套碰撞網格。以 `tests/world_harness_test.gd` 實例化驗 6 張 PASS（tracker 建立、encounter_id=mine、`_is_enc_at` 走行區 true/界外 false）。
- **boss 歸 Phase 3**（John 指示）：`nm_f`→`necro` 的 `BossMark` 涉 ch1_step 條件（魔影@step6 vs necro@step12 同一深處），與 Phase 3 过场一起做。

### CH1-05　東森 ef_* 掛遭遇 + boss　◑ 遷移+遭遇完成（boss 歸 Phase 3）
- 做什麼：（依 D3 遷移到 world_scene.gd 後）ef_* 補 `enc_group`（`forest`/`forest2`）；`ef_d`(boss房) 放 `BossMark` 接 `ch2_bear`（bear_dire，小節5 熊複戰）。小節1 的熊必敗另作定點戰。
- 依賴：D3（控制器路線）、D1。
- 驗收：ef_* 走動觸發森林遭遇；ef_d 打得到熊。
- **✅ 遷移+遭遇完成（2026-07-19）**：9 張 ef 由 `painted_screen.gd` 遷移到 `world_scene.gd`（換 script ext_resource、補 `map_w/h`＋空 `ground_tiles`/`blk_rows`＋填 `enc_rows`＋`camera_zoom=1.0`、加空 `Ground` TileMapLayer 節點〔`world_scene._fill_ground` 硬存取 `$Ground`〕、移除 Player 下靜態 alan sprite〔避免與動態 AnimatedSprite2D 雙圖〕、移除該 sprite 的 ext_resource）。`enc_group` 依連通深度配：**淺(forest)=a/b/e/f、深(forest2)=c/d/g/h/i**（Town→a→e→b(hub)→分支；forest2 多 wolf）。harness 驗 9 張 PASS、零 runtime error。
  - 註：ef 碰撞層仍是 38px（`collision_tileset.tres`、33×33），遭遇格用 world_scene 的 32px；被碰撞擋處玩家站不上、不影響遭遇判定。`ef_*.png` 為 1254px，`map_w/h=40`（camera limit 1280，略大於圖 26px，與舊 painted_screen 的 1254 觀感等價、與 nm 一致）。
- **boss 歸 Phase 3**：`ef_d`→`ch2_bear`（熊複戰）是定點戰、涉 ch1_step=11「可勝」條件，與 Phase 3 过场一起做。

### CH1-06　場景閘門/推進依新 flag 調整　◑ 部分完成（閘門旗標遷移歸 Phase 3）
- 做什麼：Town 東門/北門 `ExitZone.min_step`、各 `cut_on_enter`、`TriggerZone` 的條件全部改讀 D1 的新 flag；`save_manager.gd` 的 `VALID_RESUME_SCENES` 更新為 painted 場景名。
- 驗收：從新遊戲照 flag 序列能一路開放正確的門。
- **◑ 部分完成（2026-07-19）**：
  - ✅ `save_manager.gd` `VALID_RESUME_SCENES` 已改 painted 主線場景名（`Town`＋`NMA-NMF`＋`EFA-EFI`，對齊 SceneRouter.SCENE_PATHS）；連動修 `tests/check_save_roundtrip.gd`（改用 `NMA`，roundtrip PASS）。
  - ⏸ **閘門/过场旗標 `step`→`ch1_step` 遷移留 Phase 3**。理由：推進 ch1_step 的过场是 Phase 3 才重寫（附錄 A），Phase 2 單獨把閘門改讀 ch1_step 會讓北門（`ExitN→NMA`）永不開（沒有过场設 ch1_step≥4）→ 反而打不到 necro、牴觸 M-A；且 M-A 明文允許「过场沿用舊的/簡略」。故本節旗標語意遷移與 Phase 3 CH1-07~12 的过场改寫綁在一起做，屆時 `exit_zone.gd evaluate()`（現硬讀 `flags["step"]`）與 town 閘門一起切到 `ch1_step`。
  - ⚠ **既有缺口（非本次引入，記錄待評估）**：遊戲內所有 `save_game()` 都是零參呼叫（menu/shop/pickup/chest/dialogue），世界場景進場未寫入自己的 `scene_id` → 目前 resume 的 `scene` 恆為舊值/空、退回 Town。要真正 resume 回 painted 礦山/森林，需在世界場景進場/傳送時呼叫帶 `scene_name` 的 `save_game()`（CH1-06 明文外）。

> ▶ **里程碑 M-A**：painted 主線可從芳蕾鎮 → 礦山(序章敗戰) → 回鎮 → 東森 → 礦山(打 necro) 從頭走到尾、每段打得到該打的。過場可還是舊的/簡略。**先驗收這個手感再進 Phase 3。**
>
> ◑ **現況（2026-07-19，Phase 2 遭遇骨架）**：隨機遭遇已全線接上（北礦 mine、東森 forest/forest2，每段走動會遇怪）；`VALID_RESUME_SCENES` 已對齊 painted。**boss 定點戰（necro/熊）與線性閘門推進（ch1_step）歸 Phase 3**。John 現可實機驗「走 painted 主線＋隨機遭遇」的手感（沿用舊过场/舊 `step` 閘門即可到各場景）。

---

## Phase 3 — 劇情血肉（依 D1、D2、Phase 1；工作量大）

六小節過場重寫。內容照 [第一章劇本草稿.md](../docs/story/第一章劇本草稿.md)，格式照 [specs/DIALOGUE_SPEC.md](../specs/DIALOGUE_SPEC.md)。每節產出對應 `cuts/*.tres`（＋必要的 npc 對話 action）。

| 工項 | 小節 | 內容 | 複用/新寫 | 依賴 |
|---|---|---|---|---|
| CH1-07 | 1 勇者的日常 | 開場→森林熊必敗→亞倫登場 | **新寫**（森林熊定點敗戰） | D1 |
| CH1-08 | 2 不甘心的少年 | 鎮上武裝(漢克劍/瑪莎戒/吉德藥)→偷跟亞倫 | 半複用(give_sword/ring/potion 已有)＋新過場 | CH1-02 |
| CH1-09 | 3 礦山的那一天 | 礦山→魔影必敗→亞倫斷後→回報 | 複用序章(demon_pre/post/mine_intro)調整 | D1 |
| CH1-10 | 4 不是一個人 | 瑪琳入隊→登錄(給靴)→**三件F級委託** | 半複用(town_start/register)＋**委託全新** | D1、CH1-02 |
| CH1-11 | 5 一壺蜂蜜酒 | 老葛雷私託→守衛→吉德採蜜→**熊複戰**→放行 | **全新** | D2、CH1-01、CH1-05 |
| CH1-12 | 6 被汙染的真相 | 骷髏→**necro boss**→黑水晶爆發→瑪琳中咒→回報 | 複用 mine_truth＋necro_intro **接線**＋新(中咒) | CH1-04 |

- **CH1-12 內含 necro 接線**：`necro_intro` 補觸發點（礦山最深處）＋把 `battle` 從空改成接 `necro` 戰；勝利後接「黑水晶爆發/瑪琳中咒」新過場，設瑪琳咒印 flag。
- **flag 串接**：每節過場的 `when`/`setstep`/`once` 依 D1 藍圖串成線性。

> ▶ **里程碑 M-B**：六小節劇情從頭到尾可跑，過場、委託、道具給予、boss 都到位。

---

## Phase 4 — 整合與收尾

### CH1-13　整章串測
- headless smoke（`tests/smoke_test.gd`）過 + 寫一支第一章流程測試（照 flag 序列驅動、驗每個節點的過場/遭遇/道具觸發）；實機從新遊戲跑完整第一章。
- 驗收：無 parse error、全流程無卡關、boss 打得到、道具正確入手。

### CH1-14　同步文件
- 大綱「章節結構（現行實作）」表更新為六小節已實作；`cuts` 對照更新；`docs/README`、本檔狀態改「已驗收」。
- 依 CLAUDE.md 文件同步規則：動到劇情→同步 story docs；動數值→同步設計文件。

### CH1-15　封存舊 tile 場景
- `town(tile)/hub/forest/forest2/mine/cave/eforest1-3.tscn` 主線已不走，確認無引用後封存；同步 `smoke_test.gd` 場景清單、`save_manager.gd` 白名單。
- 低優先，M-B 後做，避免早清影響對照。

---

## 附錄 A：`ch1_step` flag 對照表（D1 定案，整章骨架）

線性主線旗標 `ch1_step`（0→13）；過場名 `s?_*` 為占位，實際命名於 Phase 3 定。定點戰＝劇情強制戰（非隨機遭遇）。

| ch1_step | 小節 | 節點 | 觸發（過場 / 遭遇 / boss / 道具 / 閘門） |
|--:|:--|:--|:--|
| 0 | 1 | 開場·路德家 | 過場 `s1_open`；開放芳蕾鎮東門→東之森 |
| 1 | 1 | 東森試身手 | 隨機遭遇（`forest` 表：bird/gslime）|
| 2 | 1 | 森林熊·**必敗** | 定點戰 `bear_dire`(必敗)→過場 `s1_bear`＋亞倫登場→`ch1_step=3` |
| 3 | 2 | 回鎮·家中 | 過場 `s2_home`；漢克贈舊劍、瑪莎贈護身戒、吉德贈藥水×2 |
| 4 | 2 | 偷跟亞倫 | 過場 `s2_follow`；開放芳蕾鎮北門→北方礦山 |
| 5 | 3 | 礦山入口·會合 | 過場 `s3_mine_in`（亞倫 guest 同行）|
| 6 | 3 | 魔影·**必敗** | 定點戰 `shadow_demon`(必敗)→過場 `s3_alan_fall`(亞倫斷後)→transfer 回鎮、`ch1_step=7` |
| 7 | 3 | 公會回報 | 過場 `s3_report`（緹娜；礦山封鎖）|
| 8 | 4 | 入隊·登錄 | 過場 `s4_join`（瑪琳入隊 party=[ludo,marin]）→登錄 `reg=1`、贈冒險者靴 |
| 9 | 4 | 三件 F 級委託 | ①米拉鏡草(`herb`0→3) ②討伐取證(狼皮×2) ③耳語(純對話)；全完成→`ch1_step=10` |
| 10 | 5 | 老葛雷私託·採蜜 | 過場 `s5_gray`＋`warden` 守衛擋＋吉德要烈火蜂蜜；開放東森採蜜區 |
| 11 | 5 | 熊複戰·放行 | 定點戰 `bear_dire`(可勝)→得烈火蜂蜜→調蜂蜜酒→放行守衛→開放礦山 |
| 12 | 6 | 重返·boss | 隨機遭遇(`mine`)＋骷髏；最深處 boss 戰 `necro`(擊退不殺) |
| 13 | 6 | 黑水晶·中咒·結章 | 過場 `s6_curse`(瑪琳咒印 `marin_curse=1`)→回鎮回報→**第一章完** |

**支線/附屬 flag**：`reg`(登錄)、`herb`(0→3 鏡草)、委託①②③完成計數、`marin_curse`(瑪琳咒印)。

## 附錄 B：缺口 ↔ 工項對照（來自 2026-07-19 稽核）

| 稽核缺口 | 工項 |
|---|---|
| 烈火蜂蜜/蜂蜜酒不在 items | CH1-01 |
| 3 裝備正名 + 冒險者靴時機 | CH1-02 |
| shop_hank 死碼 | CH1-03 |
| painted 主線 0 遭遇/0 boss | CH1-04, CH1-05 |
| M4 東森深處無場景 | （未來支線，不在第一章主線；本清單不含）|
| 六小節過場缺（1/4/5/6） | CH1-07~12 |
| necro_intro 未接線+無 battle | CH1-12 |
| 礦山守衛缺 | CH1-D2 |
| 無 quest 系統 | （設計選擇，續用 flags；委託在 CH1-10/11 用對話+flag 實作）|

## 未來擴充備忘：開場 intro（John 構思中，之後插）

- **插入點**：`title.gd` 新遊戲流程 `GameFlow.new_game()` 與 `SceneRouter.go_to(開場場景)` **之間**——在 `ch1_step 0` 起點**之前**，與第一章接線（ch1_step 0→13，皆在進場景之後）**解耦**。
- **與本清單的關係**：不影響 Phase 1–3 任何工項。唯一要求：重構時保持分層乾淨——`new_game()`＝純資料初始化、`go_to()`＝進場景、開場演出由**場景內 `ch1_step==0` 觸發**（勿把開場演出綁進 `new_game()`）。intro 之後只需在那兩行之間插一段（過場場景或 CUTS），天然只在新遊戲播、繼續冒險不播。
- **內容注意**：intro 的世界觀/前史鋪陳需與小節1 開場（路德家門前揮劍）銜接、勿重複——屬內容層，John 構思時定。
