# 地圖畫面規格（手繪畫面地圖長什麼樣）

- 版本: v2.2（2026-07-26，補定滿版 Ground 與可重用遮擋物件分層）
- 適用: 手繪畫面地圖路線（整張圖＋Godot 刷碰撞），對應 `scenes/world/painted/`；地區連通與場景建置見 [`docs/pipeline/地圖製作流程.md`](../pipeline/地圖製作流程.md)

---

## 一、工程硬規格（不符會增加接圖工，盡量遵守）

| 項目 | 規格 | 為什麼 |
|---|---|---|
| **網格單位** | 32×32 px | 碰撞格、門寬、落點全以此為單位 |
| **圖片尺寸** | **1280×1280**（=40×40 格）；同一地區所有圖同尺寸 | 邊長必須是 32 的倍數才能整除刷格（舊 1254 圖已於 2026-07-21 批次縮放至 1280）|
| **畫布模式** | **有 40×40 terrain 藍圖時一律全幅滿版**：每個像素皆由藍圖地形填滿，不留圖外深灰、島嶼輪廓或邊框。未使用藍圖的舊式地圖才可使用素色深灰（近 #3a3a3a）圖外背景 | 藍圖是碰撞與出口的真相源；全幅才能逐格對位 |
| **出入口** | 全幅地圖的出口必須直接碰到畫布邊緣，開口寬 **≥2 格（64px）**；**不要在地圖上繪製指引箭頭** | 依缺口位置對位；太窄玩家會卡 |
| **開口對位** | 相鄰兩張圖的開口要在**同一軸線位置**（A 東口的 y ≈ B 西口的 y） | 過圖時玩家視覺連續，落點不用亂搬 |
| **牆/路寬** | 牆厚 ≥1 格；主要通道寬 ≥2 格 | 1 格窄道刷碰撞後實際可走空間很緊 |
| **視角** | top-down 3/4（RPG 俯視、微透視），同地區統一 | 與 M3 現有一致 |

## 二、畫面內容禁項（畫進去就是裝飾、動不了）

- ❌ 文字、數字、格線、UI、浮水印
- ❌ 角色、NPC、怪物（引擎 sprite 另擺）
- ❌ **會互動的物件：寶箱、告示牌、採集點、傳送陣**——引擎另擺才有互動；M3 的 a 圖裡畫死的寶箱就打不開（教訓）。物件外觀與產線見 [地圖互動物件規格.md](地圖互動物件規格.md)／[`world_object_art/`](../pipeline/world_object_art/workflow.md)。
- ⭕ 純裝飾（石堆、圓木、花草、柵欄）隨便畫

## 三、遮擋圖層（會蓋住角色的物件）

原則：優先把高物件做成可重用的透明物件，交由 Godot `YSort` 排序；不要產一張需與背景像素級對齊的全畫面前景圖。

- **滿版 Ground**：背景只畫地表（草地、道路、農田、低矮植被與地面陰影），不烘入建築、樹、井架、路燈等高物件。
- **遮擋物件**：高物件以透明 PNG 獨立產出，bottom-center 為固定 anchor，放入與 Player 相同的 `YSort`。物件視覺可以超出 footprint，但 footprint 的碰撞仍以 terrain 藍圖為唯一真相。
- **例外**：只在無法拆出的特殊景深畫面，才從完成的基底圖摳出 `<id>_fg.png`；此圖不可當作可重用素材。完整架構見 [`world_object_art/遮擋物件資產架構.md`](../pipeline/world_object_art/遮擋物件資產架構.md)。

## 四、色彩規則

- **中低飽和、中對比**；整地區同一調色盤、同光照方向（頂光、柔陰影）
- **可走地面（草/路）要均勻、低細節、偏亮**；**障礙（牆/樹）比地面暗一階**——角色 sprite 才跳得出來，刷碰撞時也一眼分得出擋不擋
- 陰影不要大片橫跨走道（會誤導「這裡不能走」）

## 五、城鎮附加規格（有互動建築時）

- 門：**正面朝下、位於建築下緣中間、寬 1–2 格、門前留 ≥2 格空地**（玩家從下方走上門格進入——沿用 gen-art `building` type 的定調）
- 每棟外觀差異要明顯（顏色／屋頂形狀／招牌圖形，不用文字）
- prompt 裡明列棟數與功能，例：「6 棟：公會、旅店、神殿、鎮長宅、道具店、鐵匠鋪」

## 六、地格藍圖（terrain，選用）— 給 AI 的結構指引

可在網頁工具對一張圖手畫 40×40 逐格語意藍圖（草地／磚路／泥路／橋／河／牆／山壁／森林；出入口另為獨立標記層 `entrances`，非地形），存進 `map-def.json` 的 `terrain`。產圖時把藍圖圖檔作為**結構參考圖**，再由 `blueprint_desc.py` 輸出「版圖鎖定／全幅畫布／路線不可改／精確出口」段落注入 prompt。AI 只負責把既定格位替換成地形美術，不能重畫道路、障礙輪廓或出口。

- 藍圖同時定「哪格是什麼」與「形狀在哪裡」；畫風仍照 §一～§五，但道路彎折、河流／高地輪廓與出口接觸邊界的位置不得改動。畫面仍是整張手繪 png。
- **地格詞彙、可走/阻擋語意、資料格式、怎麼畫、導出：以 [地圖製作流程.md §2.5](../pipeline/地圖製作流程.md) 為權威**，此處不重述（避免兩地脫勾）。
- 與碰撞呼應：藍圖阻擋格（河/牆/山壁）＝不可走、導出成碰撞牆，正對應 §四「障礙比地面暗一階」——畫面暗、藍圖擋。
