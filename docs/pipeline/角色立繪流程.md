# 角色立繪流程（產圖 → 去背 → 切圖 → 整合）

- 版本: v1.0（2026-07-22，依 docs 新架構自原 `美術規格/角色立繪規格.md` v1.3 拆出流程面；「長什麼樣」見 [`docs/design/角色立繪規格.md`](../design/角色立繪規格.md)）
- 對應 skill: `gen-role-prompt`（問答收需求 → 依本檔模板組 prompt；規則以本檔＋design 規格為準，skill 不內嵌規則）
- 驗收: 一律走 CLAUDE.md「產圖驗收流程」兩階段——**先預覽給 John 驗收，過了才整合**

---

## 一、流程總覽

```
① gen-role-prompt 問答 → 組 prompt（本檔「三、Prompt 模板」）
② John 用產圖工具產圖（大圖）
③ 預覽驗收（CLAUDE.md 兩階段；要改就留在此步）
④ 去背（外部工具或 Godot import 鍵除螢光色）
⑤ 需要時用 tools/role_slicer 切圖（一張全身 → face/portrait/menuart 三張；python3 tools/role_slicer/serve.py → :8777）
⑥ 整合：縮目標尺寸 → 存 godot-project/assets/ui/ → Godot Reimport → 更新 CREDITS_素材授權.md
```

## 二、去背螢光底（讓外部去背一次乾淨的三條硬規則）

1. **背景＝單一平塗螢光純色、滿版填滿整個畫面到邊**（無漸層、無材質、無投影、**無邊框／外框／圓角卡片邊／暗角**），且**背景色不可把光暈打到角色身上**（no colored rim light／spill）。
2. **底色按角色挑**（避開角色身上出現的顏色，才不會與衣服重疊）：
   - 預設 **螢光綠 `#00FF00`**；
   - 角色有明顯**綠／青色系** → 改 **螢光洋紅 `#FF00FF`**；
   - 角色**同時有綠又有紅／粉／洋紅** → 用 **螢光青 `#00FFFF`**。
3. **沿角色身體外輪廓描一條連續深色墨線**（近黑 `#1A1A1A` 或角色主色的深色調），**2–4px**、緊貼人物外形不中斷
   ——**是順著身體形狀走的線稿，不是在畫面四周加矩形邊框**。抗鋸齒的半透明邊緣像素會混到深墨線而非亮螢光底，
   避免去背後留亮邊 halo；賽璐璐上色／動漫的軟邊也靠這條輪廓線被收成乾淨輪廓。

同一角色多張（a/b/c）：**角色描述逐字沿用**，只換「取景那一句」與比例；螢光底色三張一致（三張才會是同一個人）。

## 三、Prompt 模板（單一真相源）

> 本模板是 [`docs/design/角色立繪規格.md`](../design/角色立繪規格.md)「三、風格」＋本檔「二、去背螢光底」的 prompt 化版本
> ——**改風格或去背規則時，規格文字與本模板要一起改**（skill 只引用本檔，不另存副本）。
> `{KEY_COLOR}` 依「二」挑好後換成具體色碼，輸出不留變數。整段同一語言、不混語言。

**英文版（固定開頭）**：

```
High-quality Japanese anime JRPG character illustration, modern JRPG / light-novel illustration style: clean and refined anime line art (with line-weight variation, NOT thick heavy black outlines), delicate anime cel shading with soft gradients and clean highlights, illustration-grade finish and rich detail. Anime-style large expressive eyes, a refined, good-looking, age-honest face, well-separated hair strands. One character only, centered, facing the viewer, no frame.
Trace the character's own body contour with one clean, continuous dark (#1A1A1A) ink line about 2-4px thick that hugs the figure's silhouette (inked lineart following the edge of the body), so the character separates cleanly from the background for easy background removal. This line follows the body's shape ONLY — it is NOT a rectangle, border, or frame around the image.
Background: a single flat uniform {KEY_COLOR} fill covering the whole canvas edge to edge (full bleed) — no gradient, no texture, no shadow, no border, no frame, no rounded card edge, no vignette, and no colored light from the background spilling onto the character.
No text, no letters, no numbers, no character name, no logo, no watermark, no UI.
```

**中文版（固定開頭，與英文等義）**：

```
高品質日系動漫 JRPG 角色立繪，現代日式 RPG／輕小說插畫風：乾淨精緻的動漫線稿（線條粗細有變化、非粗黑描邊），細膩的動漫賽璐璐上色搭配柔和漸層與乾淨高光，插畫等級的完成度與豐富細節。anime 風格大而有神的眼睛、精緻好看且年齡誠實的臉、髮絲層次分明。畫面只有一個角色、置中、面向鏡頭、無外框。
沿角色身體的外輪廓描一條乾淨連續的深色（#1A1A1A）墨線，約 2-4px、緊貼人物外形（順著身體邊緣走的線稿），與背景清楚分離、方便去背；這條線只順著身體形狀走，不是在畫面四周加矩形邊界或外框。
背景：單一平塗純色 {KEY_COLOR} 滿版填滿整個畫面到邊——無漸層、無材質、無陰影、無邊框、無外框、無圓角卡片邊、無暗角，且背景色不可有光暈打到角色身上。
不要文字、字母、數字、角色名、logo、浮水印、UI。
```

**附加段**（續在固定開頭後，有才加、同語言）：

- 角色描述：英 `Character: {外觀＋服裝配色＋氣質}.`／中 `角色：{外觀＋服裝配色＋氣質}。`
- 取景（依類別擇一）：
  - a 戰鬥頭像：英 `Framing: head-and-shoulders close-up, face clearly visible, square 1:1 composition.`／中 `取景：頭到肩的特寫，臉部清楚，正方形 1:1 構圖。`
  - b 對話半身：英 `Framing: half body from the thighs up, portrait 3:4 composition, relaxed standing pose.`／中 `取景：大腿以上半身，直幅 3:4 構圖，自然站姿。`
  - c 角色全身：英 `Framing: full body head-to-toe standing pose, portrait 3:4, leave headroom above the head and space below the feet, figure centered.`／中 `取景：頭到腳的全身站姿，直幅 3:4，頭頂與腳下都留空，人物置中。`
- 姿勢／表情（有才加）：英 `Pose/expression: {...}.`／中 `姿勢／表情：{...}。`

`{KEY_COLOR}`：英 `fluorescent green #00FF00`／`fluorescent magenta #FF00FF`／`fluorescent cyan #00FFFF`；
中 `螢光綠 #00FF00`／`螢光洋紅 #FF00FF`／`螢光青 #00FFFF`。

## 四、交付檢查清單

產圖前：對照 [`design 規格`](../design/角色立繪規格.md) 挑好 type（a/b/c）、比例、該角色的螢光底色。

產圖後（走 CLAUDE.md 兩階段：**先預覽給 John 驗收，過了才整合**）：
- [ ] 比例對（a 1:1／b、c 3:4）、單人置中、無文字／外框。
- [ ] 背景是乾淨平塗螢光色、整圈深描邊封閉、角色身上無螢光色溢。
- [ ] 去背（外部工具或 Godot import 鍵除螢光色）後邊緣乾淨無 halo。
- [ ] 縮到目標尺寸，存 `godot-project/assets/ui/<檔名>`（a→`face_<id>.png` 縮 144²、b→`portrait_<id>.png`、c→`menuart_<id>.png`）；新增／覆蓋 PNG 後 Godot **Reimport** 才生效。
- [ ] 更新 `CREDITS_素材授權.md`（AI 生成，提示詞作者 John）。
- [ ] 同一角色 a/b/c 風格一致。

## 五、已驗收帳本

- **角色 a/b/c**（2026-07-18）：**全 15 位角色 a/b/c 皆已整合進 `godot-project/assets/ui/`**（新增鐵匠 `don`、反派 `necro`；`aaron→alan`／`sister→shea`／`guard→rossel` 改名完成），皆為「日系動漫精緻」風＋螢光底乾淨去背、b/c 為 3:4 直幅；舊底色版（16:9、去背不乾淨）已汰換。
- **敵人設定集全身立繪**（2026-07-19）：`wogol`、`skeleton`、`orc`、`wolf`、`bear`（`wolf` 跨東之森與礦山共用）。
- **公會懸賞圖 22 隻**（2026-07-19）：`goblin_shaman`、`goblin_tamer`、`goblin`、`goblin_chief`、`wild_hare`、`horn_hare`、`thorn_boar`、`fungus_owl`、`rotwood_beetle`、`bird`、`gslime`、`worm`、`wolf`、`bear`、`bear_dire`、`wogol`、`skeleton`、`orc`、`chort`、`necro`、`ogre`、`shadow_demon`。
- 敵人立繪未接 runtime：未來圖鑑/任務牆 UI 實作時才從 `assets-source/role/enemies/<id>/` 複製至 `godot-project/assets/`。
